apiVersion: crd.k8s.amazonaws.com/v1alpha1
kind: ENIConfig
metadata:
  name: ap-south-1a
spec:
  subnet: subnet-0b3f696da2659653b


# fro second
apiVersion: crd.k8s.amazonaws.com/v1alpha1
kind: ENIConfig
metadata:
  name: ap-south-1b
spec:
  subnet: <NEW_SUBNET_ID>

##  command
kubectl set env daemonset aws-node -n kube-system \
  AWS_VPC_K8S_CNI_CUSTOM_NETWORK_CFG=true \
  ENI_CONFIG_LABEL_DEF=topology.kubernetes.io/zone \
  ENABLE_POD_ENI=true

### this tage must be in subnet that assigned for eniconfig
  aws ec2 create-tags \
  --resources <POD_SUBNET_ID_1A> \
  --tags \
    Key=kubernetes.io/cluster/task-4,Value=shared \
    Key=kubernetes.io/role/pod,Value=1